index:
  analysis:
    analyzer:
      ik_syno:
          type: custom
          tokenizer: ik_max_word
          filter: [synonym_filter]
      ik_syno_smart:
          type: custom
          tokenizer: ik_smart
          filter: [synonym_filter]
    filter:
      synonym_filter:
          type: synonym
          synonyms_path: analysis/synonym.txt
